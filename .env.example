# Optional live inference configuration for a future provider runtime adapter.
# The deterministic workflow, Gradio demo, and bounded-model tests do not require these values.
# Never place real credentials in this file.

HF_TOKEN=your_runtime_token
MODEL_ID=your_model_id

# Local Gradio workflow checkpoint database. This file is gitignored.
WORKFLOW_DB_PATH=workflow_runs.db
